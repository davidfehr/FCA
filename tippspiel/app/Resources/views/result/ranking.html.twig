{% extends 'base.html.twig' %}

{% block custom_javascript %}
    <script type="text/javascript">
        $(document).ready(function () {

        });
    </script>

{% endblock %}            

{% block body %}
    <div class="row">
        <div class="col-sm-10 col-sm-offset-1 col-md-8 col-md-offset-2 col-lg-6 col-lg-offset-3 form-box">
            <div class="f1">
                <h4>Hier ist der aktuelle Zwischenstand</h4>
                <p style="font-size:10px;">Angaben ohne Gewähr</p>
            </div>
        </div>
    </div>     

    <div class="row">
        <div class="col-sm-10 col-sm-offset-1 col-md-8 col-md-offset-2 col-lg-6 col-lg-offset-3 form-box" >        
            <div class="f1">

                <div class="table-responsive">
                    <table class="table table-striped">
                        <thead>
                            <tr>
                                <th>Platzierung</th>
                                <th>Name</th>
                                <th>Punkte</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for user in users %}
                                <tr>
                                    <td>{{ loop.index }}</td>
                                    <td style="text-align: left;"><a href="{{ path('userDetail', { 'userId': user.id }) }}">{{ user.name }}</a></td>
                                    <td>{{ user.points }}</td>
                                </tr>
                            {% endfor %}    
                        </tbody>
                    </table>
                </div>                
            </div>
        </div>
    </div>            

{% endblock %}


